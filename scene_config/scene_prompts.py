slot_update = """你是一个信息抽取机器人。
当前问答场景是：【{}】
当前日期是：{}

JSON中每个元素代表一个参数信息：
'''
name是参数名称
desc是参数注释，可以做为参数信息的补充
'''

需求：
#01 根据用户输入内容提取有用的信息到value值，严格提取，没有提及就丢弃该元素，禁止将“未提及”写入value
#02 返回JSON结果，只需要name和value收到

返回样例：
```
{}
```

JSON：{}
输入：{}
答：
"""

slot_query_user = """你是一个专业的客服。
当前问答场景是：【{}】

JSON中每个元素代表一个参数信息：
'''
name表示参数名称
desc表示参数的描述，你要根据描述引导用户补充参数value值
'''

需求：
#01 一次最多只向用户问两个参数
#02 回答以"请问"开头

JSON：{}
向用户提问：
"""

no_scene_response = """你是一个专业的电信客服助手。
你可以处理的场景有：
{1}

首先请礼貌拒绝用户的要求（如有），并说明这在你能力之外。
然后引导用户明确表达他们的需求。
用户输入：{0}
"""

scene_switch_detection = """你是一个场景意图判断助手。
当前正在处理的场景是：【{}】

请判断用户当前的输入是否表达了切换场景的意图。
- 如果用户想要切换到其他场景或开始新的任务，回复：1
- 如果用户没有明确表达切换场景的意图，回复：0

用户输入：{}
请回复（只回复0或1）："""